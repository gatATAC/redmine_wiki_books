<h4><%= link_to_if_authorized h(book.title), :controller => 'books', :action => 'show', :id => book %></h4>
<p><em><%= format_time(book.updated_on) %></em></p>

<div class="wiki">
	<%= textilizable(truncate_lines(book.description)) %>
</div>
